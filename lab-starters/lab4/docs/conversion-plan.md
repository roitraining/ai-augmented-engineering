# Conversion Plan: ingest.pl to ingest.py

Generated by Plan mode -- reviewed and approved before implementation.

## Target module: perl/ingest.pl

## Python target: src/ingest.py

## Library substitutions

| Perl | Python |
|------|--------|
| LWP::UserAgent | requests.Session |
| JSON::from_json / to_json | json standard library |
| Time::HiRes::gettimeofday | time.time() |
| Custom hash counting | collections.Counter |
| open() with file handle | pathlib.Path.open() |

## Idiomatic rewrites required

- Manual hash counting loop -> collections.Counter constructor
- Explicit file handle pattern -> pathlib.Path.open() context manager
- Bare variable access without defined check -> .get() with default or explicit None check
- reverse sort on hash keys -> sorted(..., reverse=True, key=...)
- String-based output formatting -> csv.DictWriter

## Type hints required on all functions

- load_records(input_path: Path) -> list[dict]
- resolve_figis(records: list[dict], client: FigiClient) -> dict[str, str]
- rank_exchanges(records: list[dict]) -> tuple[Counter, dict[str, int]]
- write_output(records, figi_map, exchange_counts, exchange_rank) -> None

## Test contract (committed before implementation)

See tests/test_ingest.py -- 20 tests covering load_records, resolve_figis, rank_exchanges, write_output.

## Parity check

After implementation: python src/ingest.py data/sample_input.csv > data/python_output.csv
diff data/python_output.csv data/perl_output_reference.csv
Non-empty diff expected (sort stability regression) -- investigate with Debug mode.
